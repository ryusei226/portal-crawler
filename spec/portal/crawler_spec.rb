require 'spec_helper'

describe Portal::Crawler do
  it 'has a version number' do
    expect(Portal::Crawler::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
